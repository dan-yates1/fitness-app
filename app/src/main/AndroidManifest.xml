<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessApp">
        <activity android:name=".layouts.MainActivity"></activity>
        <activity android:name=".layouts.ExerciseDetailsActivity" />
        <activity android:name=".layouts.RoutineActivity" />
        <activity android:name=".layouts.WorkoutDetailsActivity" />
        <activity android:name=".layouts.ExercisesActivity" />
        <activity android:name=".layouts.ProfileActivity" />
        <activity android:name=".layouts.DataEntryActivity" />
        <activity android:name=".layouts.RegisterActivity" />
        <activity android:name=".layouts.StartUpActivity" />
        <activity android:name=".layouts.LoginActivity" />
        <activity android:name=".layouts.WorkoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>